"""Deprecated: replaced by src/bot/handlers/allocate.py.

The bot now uses a single shared panel (owner) account and allocates numbers
to a per-user "client" name. This module is kept only so old imports don't
break; it does nothing.
"""
